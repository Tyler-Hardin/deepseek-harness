/**
 * Client-namespace projection of the voice-context domain: a pure re-export of
 * the package's types outlet, so Client code imports only the client namespace.
 * @module @deepseek-ai/dsh-voice-context/client
 */

export type * from './types.ts'
